{
  "name": "meridian-ops-api",
  "version": "0.4.2",
  "description": "Backend API for Meridian internal order management",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest --coverage",
    "lint": "eslint src/"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "jest": "^30.0.0",
    "eslint": "^9.27.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
